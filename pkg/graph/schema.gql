type Query {
  planets: [Planet!]!
  planet(id: ID, name: String): Planet
}

type Planet {
  id: ID!
  name: String!
  comment: String!
}

type Mutation {
  createPlanet(name: String!): String
}

type Subscription {
  subscribeName: String
}
